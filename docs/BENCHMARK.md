# 基准与正确性实测

🌐 简体中文 ｜ [English](BENCHMARK.en.md)

> 2026-09-11 · Intel Core Ultra 9 185H · Node v22.12.0 · `NODE_OPTIONS=--expose-gc` · 每个场景 5 轮 + 1 热身，每轮从零构建并轮换执行顺序 · 数据为 geojson-path-finder 自带的测试路网。
> 表中为**中位数 [最小–最大]**；两组区间重叠的差异视为噪声，不下结论。复现：`pnpm bench`（`--only osm` 可单跑一个场景）。

## 参赛方

| 名称                      | 配置                                                                                                     |
| ------------------------- | -------------------------------------------------------------------------------------------------------- |
| geoverse A* / Dijkstra    | `snap.mode: 'exact'`，与对手一样要求输入是路网顶点                                                       |
| geoverse A\* + edge snap  | 默认线段吸附：每个途经点多一次 R 树最近邻 + 叠加层                                                       |
| geojson-path-finder 2.1.0 | `tolerance: 1e-9`，保持与其他库相同的拓扑（仅完全相同的坐标相连）                                        |
| terra-route 0.0.18        | A\* + ALT 地标（第 256 次查询后启用）；不支持权重，只参加最短距离场景，代价由其返回几何按 haversine 重算 |

"非最短"列 = 该库代价高于所有参赛方中最优值的点对数（相对误差 1e-6）。本库在测试里与独立裁判逐对相等（见 `test/gpf-parity.test.ts`），所以这一列非零即说明该库给出了非最短路。

## 1. network.json — 最短距离

44 条线、932 坐标，压缩后只剩 17 个节点。

| 库                       | 初始化 ms         | 300 次路由 ms      | 单次 µs p50 / p95 | 找到 | 非最短 |
| ------------------------ | ----------------- | ------------------ | ----------------- | ---- | ------ |
| geoverse A\*             | 1.9 [1.7–2.9]     | 12.9 [10.9–15.6]   | 34 / 110          | 300  | 0      |
| geoverse Dijkstra        | 1.8 [1.2–2.6]     | **8.3** [7.1–13.8] | 21 / 74           | 300  | 0      |
| geoverse A\* + edge snap | 1.5 [1.2–2.1]     | 21.0 [17.9–23.6]   | 47 / 142          | 300  | 0      |
| geojson-path-finder      | 10.6 [8.9–11.5]   | 20.5 [18.4–24.5]   | 60 / 117          | 300  | 0      |
| terra-route              | **0.4** [0.4–0.7] | 17.6 [16.0–19.2]   | 38 / 169          | 300  | 0      |

图极小时 A\* 的启发式开销划不来，Dijkstra 最快；两者都与 terra-route 区间不重叠。线段吸附在这里占了单次查询的大头（两次最近邻 + 叠加层 ≈ 27 µs）。

## 2. large-network.json — 最短距离（所有道路类型）

20120 条线、13.5 万坐标（哥德堡 OSM，基准前滤掉 107 个面要素）。

| 库                       | 初始化 ms               | 300 次路由 ms             | 单次 µs p50 / p95 | 找到 | 非最短 |
| ------------------------ | ----------------------- | ------------------------- | ----------------- | ---- | ------ |
| geoverse A\*             | 310.0 [283.5–350.0]     | **306.3** [287.0–337.0]   | 705 / 2948        | 300  | 0      |
| geoverse Dijkstra        | 304.1 [248.5–353.1]     | 692.7 [681.6–733.7]       | 2282 / 4824       | 300  | 0      |
| geoverse A\* + edge snap | 207.1 [194.7–301.4]     | 327.3 [310.4–377.1]       | 781 / 2975        | 300  | 0      |
| geojson-path-finder      | 2798.3 [2724.1–2869.1]  | 11574.0 [11431.0–12882.6] | 39143 / 80185     | 300  | **41** |
| terra-route              | **112.8** [105.6–118.9] | 1410.8 [1291.2–1433.2]    | 2964 / 13280      | 300  | 0      |

- 路由：A\* 比 terra-route 快约 4.6 倍、比 geojson-path-finder 快约 38 倍，区间均不重叠；比本库 Dijkstra 快约 2.3 倍。
- 初始化：比 geojson-path-finder 快约 9 倍；比 terra-route 慢约 2.7 倍——本库多做了权重、链压缩、连通分量、A\* 嵌入与 R 树。
- 前三行的初始化是**完全相同的代码**，中位数却在 207–310 ms 之间摆动：这正是"单轮数字不能下结论"的现场证据。线段吸附相对 exact 的路由差（327 vs 306）区间重叠，同样不下结论。
- geojson-path-finder 即使在**纯距离**权重下也有 41/300 对非最短，根因见 §4。

## 3. large-network.json — OSM 通行时间、单向（geojson-path-finder 自带测试权重）

权重函数原样移植自 geojson-path-finder 的 `test/osm-weight.js`（`bench/osm-weight.ts`），双方调用同一个函数；点对从该权重下的最大连通分量里抽样。

| 库                       | 初始化 ms              | 300 次路由 ms           | 单次 µs p50 / p95 | 找到 | 非最短 |
| ------------------------ | ---------------------- | ----------------------- | ----------------- | ---- | ------ |
| geoverse A\*             | 264.9 [176.7–268.4]    | **206.0** [195.8–215.0] | 552 / 1678        | 293  | 0      |
| geoverse Dijkstra        | 261.0 [190.2–413.9]    | 285.4 [266.8–288.4]     | 880 / 1871        | 293  | 0      |
| geoverse A\* + edge snap | 229.8 [182.9–262.0]    | 226.7 [213.0–252.7]     | 633 / 1737        | 293  | 0      |
| geojson-path-finder      | 2708.8 [2675.5–2768.0] | 6678.5 [6574.8–6764.8]  | 21498 / 43935     | 293  | **42** |

- 时间权重下启发式按"全网最快车速"缩放、比距离场景弱，A\* 仍比 Dijkstra 快约 1.4 倍（区间不重叠），比 geojson-path-finder 快约 32 倍。
- **吸附的价值**：最初一版基准误从"所有道路类型"的分量里抽点，大量点落在被该权重判为不可通行的人行道上——exact 模式只有 74/300 能路由，默认线段吸附把这些点挪到最近的可通行道路，292/300 能路由。

## 4. geojson-path-finder 非最短路的根因实验（`pnpm bench:gpf`）

同一批 60 个随机点对（OSM 时间权重），与不共享任何代码的朴素裁判（未压缩有向图 + Dijkstra）比较：

| 变体                                                        | 非最短 |
| ----------------------------------------------------------- | ------ |
| geojson-path-finder 2.1.0 原版                              | 7      |
| 同一库的 Dijkstra 跑它**未压缩**的顶点图                    | 0      |
| 只放宽 `compactor` 里一行守卫（允许更便宜的旁路替换已有边） | 0      |
| geoverse-line-finder                                        | 0      |

| 点对 | 裁判 (s) | geojson-path-finder | 未压缩图 | 单行补丁 | geoverse |
| ---- | -------- | ------------------- | -------- | -------- | -------- |
| 6    | 686.5    | 687.7 (+0.18%)      | 686.5    | 686.5    | 686.5    |
| 23   | 280.1    | 281.3 (+0.44%)      | 280.1    | 280.1    | 280.1    |
| 33   | 232.9    | 233.0 (+0.02%)      | 232.9    | 232.9    | 232.9    |
| 46   | 446.6    | 449.3 (+0.62%)      | 446.6    | 446.6    | 446.6    |
| 52   | 587.4    | 604.4 (+2.89%)      | 587.4    | 587.4    | 587.4    |
| 53   | 453.8    | 454.2 (+0.09%)      | 453.8    | 453.8    | 453.8    |
| 56   | 437.5    | 440.3 (+0.63%)      | 437.5    | 437.5    | 437.5    |

结论：geojson-path-finder 压缩度 2 顶点时，`if (!neighbor[otherNeighborKey] && weightFromNeighbor)` 只在两邻点间**没有边**时才添加旁路；已有更贵的边（平行道路、单向对向车道、或先前压缩出的更长链）时，更便宜的旁路被丢弃，最短路从压缩图里消失。用裁判重算它返回的路径恰好等于它报出的权重——路径合法，只是不是最短。2.0.2 与 2.1.0 为同一行代码。

## 5. 0.2.0 功能基准（`pnpm bench:features`）

> 2026-09-14，机器与方法同上；0.1.0 对照组是 npm 上发布的包（devDependency 别名 `glf-baseline`）。对 0.1.0 的回归比较的是**构建产物**（`dist/`），先 `pnpm build`；在 tsx 下直接跑源码会让建图看起来慢约两成。

### 5.1 默认配置对 0.1.0（9 轮）

| 场景                                 | 版本  | 初始化 ms           | 全部查询 ms         | 单次 µs p50 / p95 | 找到 | 权重不一致 |
| ------------------------------------ | ----- | ------------------- | ------------------- | ----------------- | ---- | ---------- |
| large-network 距离，线段吸附         | 0.1.0 | 236.4 [183.6–249.4] | 284.4 [197.9–310.9] | 671 / 2495        | 300  | 0          |
|                                      | 0.2.0 | 251.2 [204.0–287.1] | 338.3 [208.6–367.4] | 781 / 2921        | 300  | 0          |
| large-network OSM 通行时间，线段吸附 | 0.1.0 | 202.3 [181.7–231.5] | 222.2 [208.6–274.1] | 619 / 1940        | 291  | 0          |
|                                      | 0.2.0 | 234.4 [195.4–250.6] | 232.1 [218.1–256.4] | 621 / 2040        | 291  | 0          |
| 合成仓库 292 任务 × 8 途经点         | 0.1.0 | 1.4 [1.0–2.4]       | 81.4 [68.2–96.2]    | 239 / 560         | 292  | 0          |
|                                      | 0.2.0 | 1.5 [0.9–2.4]       | 91.9 [66.1–104.0]   | 260 / 582         | 292  | 0          |

- 所有区间都重叠，按规矩**不下"变慢"的结论**；权重逐条一致，金样本测试另外保证路径与 `settled` / `relaxed` 逐位相同。
- 距离场景的查询中位数差最大，因此另用同一进程、交替执行的微基准复核（300 对、12 轮）：0.1.0 为 304.5 [197.3–327.7] ms，0.2.0 为 317.6 [208.3–339.1] ms，定标节点数同为 1,166,663——同样落在噪声里。
- 开发中两次被基准抓到的真实开销，都已修掉：里程最初沿原始坐标重算一遍距离，建图慢约一成（改为复用权重循环里的长度）；候选描述（关联要素、侧向、里程）最初每个命中都立即构建（改为按需构建）。

### 5.2 有向 ALT 地标（5 轮，线段吸附，初始化含地标准备）

| 权重         | 引擎                    | 初始化 ms           | 全部查询 ms             | 单次 µs p50 / p95 | 找到 |
| ------------ | ----------------------- | ------------------- | ----------------------- | ----------------- | ---- |
| OSM 通行时间 | A\*                     | 198.8 [184.8–224.9] | 223.3 [169.1–244.3]     | 533 / 1959        | 293  |
|              | A\* + ALT（8，活跃 4）  | 220.3 [198.1–293.7] | **106.7** [102.0–118.2] | 234 / 1033        | 293  |
|              | A\* + ALT（16，活跃 4） | 272.9 [252.1–312.6] | 97.8 [69.3–123.0]       | 215 / 915         | 293  |
| 距离         | A\*                     | 209.1 [174.8–286.2] | 283.8 [278.5–296.4]     | 666 / 2805        | 295  |
|              | A\* + ALT（8，活跃 4）  | 370.5 [324.4–392.4] | **172.3** [160.4–183.0] | 389 / 1679        | 295  |
|              | A\* + ALT（16，活跃 4） | 388.7 [328.2–484.5] | 158.2 [142.0–167.5]     | 375 / 1399        | 295  |

- 对照有向 ALT 设计的验收判据：OSM 时间场景 8 个地标提速 2.09 倍（≥ 2 倍，区间不重叠）；距离场景 1.65 倍（要求不变慢）；OSM 图上准备 8 个地标 37.9 [33.5–43.5] ms、1.6 MB（要求 ≤ 300 ms、≤ 10 MB）。所有查询权重与 A\* 一致。
- 16 个地标比 8 个只再快约一成，准备时间与内存翻倍，所以默认 8 个、每查询只用 4 个。

### 5.3 没有启发式时的双向 Dijkstra（9 轮，自定义度量不提供嵌入）

| 引擎          | 初始化 ms           | 全部查询 ms             | 单次 µs p50 / p95 | 找到 |
| ------------- | ------------------- | ----------------------- | ----------------- | ---- |
| Dijkstra      | 263.8 [180.2–365.2] | 727.0 [711.6–827.9]     | 2390 / 5305       | 297  |
| 双向 Dijkstra | 243.4 [185.6–415.8] | **652.0** [514.1–710.2] | 1859 / 4651       | 297  |

- 总耗时快约一成、单次中位数快约两成，9 轮区间刚好分开（5 轮时仍有 2.5 ms 重叠），属于"小幅但真实"。有嵌入时 A\* 远比它快，所以它不是默认引擎，只作为可注册的选项。
- 第一版实现每次松弛都调用闭包、每次展开都查 `Map`，比单向 Dijkstra 慢 1.7 倍；改为内联循环后才有上面的结果。

### 5.4 全程择优的开销（AC7，5 轮，合成仓库 292 任务 × 8 途经点）

| 配置                                  | 全部查询 ms         | 单次 µs p50 / p95 | 找到 |
| ------------------------------------- | ------------------- | ----------------- | ---- |
| 最近选择（`connectivity: 'nearest'`） | 49.6 [47.8–57.0]    | 152 / 298         | 292  |
| 全程择优 K = 1                        | 60.7 [59.5–76.4]    | 194 / 388         | 292  |
| 全程择优 K = 4（默认）                | 100.2 [92.7–105.7]  | 291 / 716         | 292  |
| 全程择优 K = 8                        | 132.4 [126.9–154.5] | 403 / 937         | 292  |

K = 4 约为最近选择的 2.0 倍（要求 ≤ 5 倍）。K = 1 的择优走的是多源多汇搜索的通用路径，比最近选择慢约两成；默认配置仍走最近选择的原路径（见 5.1）。

### 5.5 全程择优相对最近吸附能省多少（计数型，单次评估，不是计时）

每个场景 300 对随机点（偏离道路 5–40 m），比较"路网代价 + 两端吸附代价"的合计。

| 路网               | 吸附代价            | 最近吸附不可达、择优可达 | 收益 p50 / p90 / 最大   | 收益 > 1% / > 10% |
| ------------------ | ------------------- | ------------------------ | ----------------------- | ----------------- |
| OSM 通行时间，单向 | 距离按 10 km/h 换算 | 7 / 300                  | 0.07% / 12.82% / 86.19% | 111 / 35          |
| 距离（km），双向   | 距离                | 5 / 300                  | 0.24% / 2.17% / 36.90%  | 54 / 2            |

- 单向路网上收益主要来自"最近的那条路方向不对"，这是全程择优与仓库场景无关的通用价值。
- 双向距离场景收益小，而且其中一部分是吸附腿走直线在"抄近路"；因此 `costMode: 'none'` 时择优会偏向远处候选，建议配合 `'ends'` / `'arrive-depart'` 与 `maxRelocation`（见 ARCHITECTURE §5.5）。

### 5.6 楼层感知的 A\* 下界（`--only levels`，5 轮）

合成楼栋：30 层，每层 7 × 7 走廊网格（间距 5 m，按长度计价），2 部逐跳电梯（每跳代价 4），共 1 350 个节点，`perLevel` = 4.00。行程是"从 F1 到正上方的 Fk"，k = 2…30，共 29 条。

展开节点数（`leg.settled`，确定性，单次评估即可）：

| 行程                 | Dijkstra | A\*（只有平面下界） | A\* + 楼层下界 | A\* + 楼层下界 + ALT(8) |
| -------------------- | -------- | ------------------- | -------------- | ----------------------- |
| F1 → F5              | 33       | 13                  | 5              | 5                       |
| F1 → F10             | 186      | 84                  | 10             | 10                      |
| F1 → F20             | 632      | 433                 | 20             | 20                      |
| F1 → F30             | 1 082    | 883                 | 30             | 30                      |
| F1 → F30，对角另一端 | 1 351    | 1 345               | 1 231          | 1 223                   |

计时（29 条行程合计）：

| 引擎                    | 建图 ms        | 全部查询 ms   | 单次 µs p50 / p95 | 权重不一致 |
| ----------------------- | -------------- | ------------- | ----------------- | ---------- |
| Dijkstra                | 4.7 [3.9–5.3]  | 4.3 [3.9–5.4] | 116 / 294         | 0          |
| A\*（只有平面下界）     | 4.1 [3.6–5.1]  | 3.5 [2.6–6.9] | 103 / 342         | 0          |
| A\* + 楼层下界          | 4.3 [3.7–10.1] | 2.4 [2.0–3.4] | 73 / 162          | 0          |
| A\* + 楼层下界 + ALT(8) | 7.9 [6.8–10.5] | 2.7 [2.3–3.8] | 85 / 185          | 0          |

- 展开数是这一节的主要结论：竖向行程上楼层下界把展开数压到"每层约一个节点"，F1 → F30 为 883 → 30。RFC-0015 的验收线是"≤ A\* 的 1/5"，实测为 1/29。
- 计时只有相对 Dijkstra 的区间不重叠（2.0–3.4 对 3.9–5.4 ms）；与只有平面下界的 A\* 区间重叠，在这个规模（单次搜索 ~100 µs）上算噪声。单次搜索太快，真正的差距要在更大的楼栋、或一对多 / 代价矩阵这类批量查询里才显出来。
- **最后一行是边界**：目标在平面上也很远时几乎没有收益。原因在平面项——走廊是曼哈顿网格，直线距离下界比真实步行距离少约 √2 倍，留下的松弛让大部分节点仍像在最短路上；ALT 也只帮了约 1%。这是网格路网对 A\* 的固有难点，与楼层层无关。
- 零代价电梯或缺 `ordinal` 的分组会让 `perLevel` 退化为 0，此时与"只有平面下界"完全相同（见 [MULTI_LEVEL.md](MULTI_LEVEL.md) §7.3）。

## 复现注意

- 不要与测试、构建或另一个基准并行跑；笔记本注意电源模式。
- 数据目录查找顺序见 `bench/README.md`；换机器时 npm 包自带的 `node_modules/geojson-path-finder/test` 也能用。
- 原始输出保存在 `bench/results/<时间戳>.{md,json}`（不入库）。
